# ============ 环境变量与密钥 ============
# .env 含真实密钥，必须排除；.env.example 为模板，保留提交
.env
.env.*
!.env.example

# ============ 用户数据目录（云端无此数据，运行时在 CI 内重新生成） ============
data/progress/
data/mastery/
data/daily-intel/

# ============ Python 缓存 ============
__pycache__/
*.py[cod]

# ============ 临时文件与备份 ============
.kb-tmp/
*.bak*

# ============ Hermes Agent 本地配置 ============
.hermes/
