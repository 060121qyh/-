# TA-001 工作日志 — REQ-20260813-001

- **任务ID**：REQ-20260813-001
- **工号**：TA-001（技术执行）
- **指派方**：PM-001（见 `outputs/需求评审-REQ-20260813-001.md`）
- **开始时间**：2026-08-13 06:05（北京时间）
- **承接任务**：T1 题库扩充 / T2 知识库每日更新机制 / T3 个性化实时情报采集 / T4 云端推送

---

## 执行步骤（增量追加）

### 1. 前期勘察（06:05–06:20）
- 阅读 PM 评审文档，确认 4 任务验收口径（阶段一新增 29 题 → 总量 44 题）。
- 勘察现有题库：`data/quiz-bank/2026-08-12-001.json` 15 题全为「时政热点」，字段 12 个（type/module/difficulty/stem/options/answer/explanation/correct_answer/term_breakdown/option_analysis/exam_hint/mnemonic）。
- 勘察知识卡：`data/knowledge-cards/` 9 模块目录 11 张卡，已通读河南省情/法律/马哲/中特/政府工作报告/中央会议/三农卡，作为出题素材底座。
- 勘察旧系统：`~/hermes-study-mentor/push_intel.py`（本机版飞书推送）、`.github/workflows/daily-intel.yml`（云端模板，7/12/19 三定时）。
- 环境确认：python 3.11.15 可用；gh CLI **未安装**（GitHub 推送走 git remote 方案）；git 2.55 可用。

### 2. T1 题库扩充（进行中）
- [ ] 编写 `scripts/expand_quiz_bank.py`（增量扩充 + 五段式完整性校验 + 去重）
- [ ] 编写 `data/quiz-bank/2026-08-13-001.json`（29 题：时政+5 / 省情+8 / 法律+6 / 马哲+5 / 中特+5）
- [ ] 运行校验脚本，通过后登记

### 3. T3 情报采集
- [ ] 编写 `scripts/intel_collector.py` 并实际运行

### 4. T2 每日知识库更新
- [ ] 编写 `scripts/daily_kb_update.py` 并实际运行

### 5. T4 云端推送（本轮收尾 06:20–06:50）
- [x] 编写 `.gitignore`（并行 subagent 06:08 追加 `data/daily-intel/`、`.hermes/` 忽略项，合理保留）
- [x] 编写 `.github/workflows/daily-intel.yml`
- [x] 编写 `scripts/push_intel.py`（云端版：os.environ 读 FEISHU_APP_ID/FEISHU_APP_SECRET/FEISHU_CHAT_ID，缺失 exit 1；读最新 intel-*.json，无则降级重跑 intel_collector.py；取前 8 条最相关；去 localhost:8899 链接）
- [x] 本机真实密钥测试推送 → **成功**，message_id=`om_x100b68faa2f508acc4c5e97b20f98f0`
- [x] git init + commit（b72bc73）+ push 尝试（失败，见下）

#### T4 真实运行结果（06:35，本机）
- 环境变量注入：`set -a && source <hermes profile .env> && set +a && export FEISHU_CHAT_ID=$FEISHU_HOME_CHANNEL`
  （坑：bash 中 source 的赋值默认不 export 给子进程，必须 `set -a` 或显式 export）
- 运行 `python scripts/push_intel.py`：
  - 使用情报文件 `data/daily-intel/intel-2026-08-13.json`（最新一期，未触发降级重采集）
  - 摘要条目 8 条（score 降序取前 8，均来自河南省人事考试网/新华网/人民网）
  - 飞书 tenant_access_token 获取成功 → interactive 卡片发送成功
  - **返回 message_id=om_x100b68faa2f508acc4c5e97b20f98f0**
- 密钥未写入任何代码/日志/提交。

#### T4 版本变更与复核（06:55）
- 并行 subagent 将 `scripts/push_intel.py` 升级为更完善版本（分类「招考/河南/三农/时政」+建议动作分档+`--dry-run`/`--date` 参数+网络重试 exit 2+md 兜底解析），已随 commit 0b4ccfe 入库。
- workflow 同步升级为两步式：先 `intel_collector.py --min-score 2` 采集，再 `push_intel.py` 推送（云端闭环，职责分离；「降级重跑采集器」由 workflow 采集步骤承担）。
- **用磁盘当前版本（==已提交版本）复核真实推送：成功，message_id=`om_x100b68fb4dab28a4b21f840aa854056`**（此前 om_x100b68fa… 为旧版测得）。
- 分支验证：`--dry-run` exit 0 ✓；缺密钥 exit 1 ✓；无当日情报跳过推送 exit 0 ✓。
- `hermes verify --json`：Flask 应用 bootstrap + 就绪 200，ok=true。

#### GitHub 推送状态（06:45）
- `gh` CLI 未安装（复核确认）；本机无 credential helper、无 GH_TOKEN/GITHUB_TOKEN。
- 已 `git init` → `git add -A` → 首次 commit `b72bc73`（131 文件，工作区干净）。
- 已 `git remote add origin https://github.com/your-github-username/multi-agent-study-mentor.git`。
- 首次 push 尝试：**失败**。确切错误：
  `fatal: could not read Username for 'https://github.com': terminal prompts disabled` + `User cancelled dialog`（git 需认证但无凭据）；且远端仓库尚未在 GitHub 创建。
- 网络本身通畅（curl github.com = HTTP 200，0.03s）。
- **结论：阻塞原因 = ① 远端私有仓库未创建；② 本机无 GitHub 凭据（无 gh CLI / credential helper / PAT）**。
- 用户手动操作命令清单见下节「遗留问题」。

#### .gitignore 隔离验证（git check-ignore 权威验证，06:48）
- ✔ `.env`、`data/progress/`、`data/mastery/`、`data/daily-intel/`、`__pycache__/`、`.kb-tmp/` 均被忽略
- ✔ `.env.example` 未被忽略（模板保留提交）
- ✔ 已提交内容扫描无任何密钥（git grep 暂存区：无 cli_/oc_/sk- 模式）
- ⚠ `server/api/quiz.py.bak2` 因并行 subagent 将规则改回 `*.bak` 而未被忽略，但保持未跟踪（不入库），已从暂存区移除；建议统一为 `*.bak*`

---

## 产出文件路径

| 任务 | 文件 |
|------|------|
| T1 | `scripts/expand_quiz_bank.py`、`data/quiz-bank/2026-08-13-001.json` |
| T2 | `scripts/daily_kb_update.py`、`data/daily-intel/YYYY-MM-DD.md` |
| T3 | `scripts/intel_collector.py`、`data/daily-intel/intel-YYYY-MM-DD.json` |
| T4 | `.gitignore`、`.github/workflows/daily-intel.yml`、`scripts/push_intel.py` |

---

## 遇到的问题
1. **bash source 不导出环境变量**：`source .env` 后 Python `os.environ` 读不到 FEISHU_APP_ID/FEISHU_APP_SECRET（shell 变量未 export）。解决：`set -a` + `source` + `set +a`。
2. **push 认证失败**：本机无 GitHub 凭据（无 gh CLI、无 credential helper、无 token），git 提示被禁用 → `could not read Username`。需用户创建私有仓库并配置凭据后手动推送（命令清单见报告）。
3. **并行 subagent 冲突**：`.gitignore` 在 06:08 被并行 subagent 改写（新增 data/daily-intel/、.hermes/ 忽略），并将 `*.bak*` 回退为 `*.bak`，导致 `server/api/quiz.py.bak2` 无法被规则覆盖（保持未跟踪，不入库）。
4. **分支名**：git init 默认分支为 `master`，push 需用 `git push -u origin master`（非 main）。

## 完成时间
- T4 收尾完成：2026-08-13 06:50（北京时间）；message_id=`om_x100b68faa2f508acc4c5e97b20f98f0`；commit `b72bc73`。
- 推送至 GitHub：**待用户手动完成**（阻塞：远端仓库未创建 + 无凭据）。

---

### 6. KM 审核问题修复 — KM-ISSUE-2026-02/03（老库第10/15题五段式字段缺陷，2026-08-13）
- **背景**：KM-001 审核报告（`outputs/km-内容质量审核-REQ-20260813-001.md` 1.2 节）判定老库五段式完整率 13/15，第10/15题不达标；合并库 `2026-08-13-merged.json` 亦存在同样缺陷。
- **第10题**「2025年经济工作九项任务之首」（单选）：`option_analysis` 为空 → 补写【选项辨析】段（A 新质生产力=第②项 ❌ / B 防风险=第⑤项 ❌ / C 全方位扩大国内需求=九项之首 ✅ / D 城镇化和乡村振兴=第⑥项 ❌），并在 `explanation` 的九项任务排序段后同步插入该段。
- **第15题**「乡村振兴措施逐项判断」（不定项）：① `correct_answer` 错位（562字符【逐项排查】整段塞入）→ 精简为 `【正确答案】ABC`；② `term_breakdown` 空 → 补术语拆解（"公司+合作社+农户"利益联结 / 农村人居环境整治提升行动 / 防止返贫动态监测 / 耕地"非农化""非粮化"）+ 题型说明；③ `option_analysis` 空 → 移入原【逐项排查】A-E 逐项辨析内容（标题规范为【选项辨析】）；④ `explanation` 按"正确答案+术语拆解+选项辨析+考情提示+记忆口诀"五段重拼。
- **修复脚本**：`scripts/fix_quiz_20260813.py`（修复前自动备份 → 定位第10/15题 → 断言修复前状态 → 写回 → 复用 expand_quiz_bank 校验）。
- **校验函数口径修正**（`scripts/expand_quiz_bank.py`）：原 len≥8 规则与精确段标题匹配对老库正常格式误报（KM 报告明确 `correct_answer` 短格式 7-14 字符为正常、带修饰语标题如【术语拆解——逐字解释…】为合法）→ 修正为：`correct_answer` 按"非空+含【正确答案】标记+含答案字母"判定，其余四字段保持长度≥8；explanation 段标题改前缀匹配。
- **校验结果**：老库 15 题 ✅ 通过；重新合并（老库15 + 新库29，去重跳过0）→ 合并库 **44 题五段式缺失数 = 0** ✅ 通过。
- **两题修复后摘要**：第10题 `correct_answer=【正确答案】C`，五字段全部非空；第15题 `correct_answer=【正确答案】ABC`，`term_breakdown`=442字符、`option_analysis`=492字符（原逐项排查），五字段全部非空；合并库两题与老库一致。
- **commit**：见 git log（REQ-20260813-001: 修复老库第10/15题五段式字段缺陷）。

## 完成时间（更新）
- KM 问题修复完成：2026-08-13（北京时间）；commit 见 git log。
